{
  "name": "wavearea",
  "version": "1.1.0",
  "description": "Edit audio in textarea",
  "main": "index.js",
  "scripts": {
    "start": "esbuild ./src/wavearea.js ./src/worker.js ./src/decoder/*.js --bundle --external:./src/decoder/* --sourcemap --loader:.svg=text --format=esm --outdir=dist --servedir=.",
    "build": "rm -rf ./dist/* && esbuild ./src/wavearea.js ./src/worker.js ./src/decoder/*.js --bundle --external:./src/decoder/* --minify --sourcemap --loader:.svg=text --format=esm --outdir=dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/audio-lab/wavearea.git"
  },
  "keywords": [
    "audio",
    "web-audio",
    "waveform",
    "dsp"
  ],
  "author": "ΔY <dfcreative@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/audio-lab/wavearea/issues"
  },
  "homepage": "https://github.com/audio-lab/wavearea#readme",
  "dependencies": {
    "@wasm-audio-decoders/flac": "^0.1.2",
    "@wasm-audio-decoders/ogg-vorbis": "^0.0.1",
    "audio-buffer": "^5.0.0",
    "audio-type": "^1.0.2",
    "kv-storage-polyfill": "^2.0.0",
    "mpg123-decoder": "^0.4.2",
    "ogg-opus-decoder": "^1.5.2",
    "opus-decoder": "^0.6.2",
    "sprae": "^2.12.3"
  },
  "devDependencies": {
    "esbuild": "^0.16.12"
  }
}
